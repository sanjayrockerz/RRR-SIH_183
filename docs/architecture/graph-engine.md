# Graph engine

The initial graph is a bounded NetworkX `DiGraph` constructed by BFS. Every edge retains chain, hash, block, timestamp, asset, amount, source, destination, and provider. Hop and node limits are mandatory request parameters.
