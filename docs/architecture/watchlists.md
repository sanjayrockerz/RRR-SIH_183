# Watch Targets

`watch_targets` represents an investigator-authorized address watch within a case. Status is `ACTIVE`, `PAUSED`, `STOPPED`, or `ERROR`. The database enforces one target per case, chain, and address. Missing provider configuration never appears active.
