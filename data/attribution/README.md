# Attribution data boundary

This directory is intentionally empty of production labels until a real, publicly documented dataset is selected and reviewed. Importers must validate chain, normalized address, entity, source, source reference, role, and confidence before persistence. Test fixtures live only in `apps/api/tests` and are marked `TEST FIXTURE` in their source type.
