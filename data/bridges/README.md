# Curated bridge registry

`bridge_registry.json` is intentionally not bundled with unverified contract addresses. Add only definitions reviewed against an approved public protocol source. Each definition must include source and version metadata. Until a file is supplied, the API reports bridge detection as an available analytical boundary but produces no bridge interaction labels.
