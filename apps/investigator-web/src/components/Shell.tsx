import type {ReactNode} from 'react';
type Props={route:string;onNavigate:(route:string)=>void;children:ReactNode};
const primary=[['dashboard','Dashboard','D'],['cases','Cases','C'],['investigate','Investigations','+'],['wallets','Wallet intelligence','W'],['graph','Transaction graph','G'],['patterns','Risk intelligence','R'],['monitoring','Real-time retracing','T'],['entities','Entities / VASPs','E'],['alerts','Alerts','!'],['evidence','Evidence','V'],['reports','Reports','P']];
export function Shell({route,onNavigate,children}:Props){return <div className="shell"><header className="topbar"><button className="brand-button" onClick={()=>onNavigate('dashboard')}><span className="brand-mark">R</span><span><b>RRR</b><small>REAL-TIME RETRACING</small></span></button><div className="global-search"><span>⌕</span><input aria-label="Global search" placeholder="Search wallet, transaction, case or entity"/><kbd>⌘ K</kbd></div><div className="top-actions"><span className="system-chip"><i/> SYSTEM OPERATIONAL</span><button className="icon-button" aria-label="Alerts" onClick={()=>onNavigate('alerts')}>!</button><span className="user">AR</span></div></header><div className="shell-body"><aside className="sidebar"><div className="side-caption">WORKSPACE</div><nav>{primary.map(([id,label,icon])=><button key={id} className={route===id?'nav-item active':'nav-item'} onClick={()=>onNavigate(id)}><span className="nav-icon">{icon}</span>{label}{id==='alerts'&&<em>3</em>}</button>)}</nav><div className="side-caption integrations">EXTERNAL SYSTEMS</div><button className="integration-link" onClick={()=>onNavigate('intake')}><span className="integration-status simulated"/>NCRP <small>SIMULATED</small></button><button className="integration-link" onClick={()=>onNavigate('intake')}><span className="integration-status simulated"/>SAHYOG <small>SIMULATED</small></button><button className="integration-link"><span className="integration-status off"/>VASP NETWORK <small>NOT CONFIGURED</small></button><div className="sidebar-footer"><span className="live-indicator"/>Historical provider data<br/><b>Alchemy · Ethereum</b></div></aside><main className="workspace"><div className="breadcrumbs"><span>RRR</span><b>/</b><span>{route==='dashboard'?'COMMAND CENTER':route.toUpperCase()}</span></div>{children}</main></div></div>}
