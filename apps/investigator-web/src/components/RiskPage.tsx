import {useEffect,useState} from 'react';
import type {Case,RiskAlert,RiskAssessment,RiskDelta,RiskFactor,Trace} from '../types';
import {api} from '../api';

function PanelTitle({eyebrow,title}:{eyebrow:string;title:string}){return <div className="panel-title"><div className="eyebrow">{eyebrow}</div><h3>{title}</h3></div>}
function Empty({label,text}:{label:string;text:string}){return <div className="empty-block"><span>◇</span><b>{label}</b><p>{text}</p></div>}

export function RiskPage({caseData,trace,onNavigate}:{caseData:Case;trace:Trace;onNavigate:(route:string)=>void}){
 const [assessment,setAssessment]=useState<RiskAssessment|null>(null);const [delta,setDelta]=useState<RiskDelta|null>(null);const [factors,setFactors]=useState<RiskFactor[]>([]);const [alerts,setAlerts]=useState<RiskAlert[]>([]);const [busy,setBusy]=useState(false);const [error,setError]=useState('');
 async function load(assess:boolean){setBusy(true);setError('');try{const current=assess?await api.assessRisk(caseData.case_id,trace.trace_id):await api.risk(caseData.case_id);setAssessment(current);if(current){const [nextDelta,nextFactors,nextAlerts]=await Promise.all([api.riskDelta(caseData.case_id),api.riskFactors(caseData.case_id,current.assessment_id),api.riskAlerts(caseData.case_id)]);setDelta(nextDelta);setFactors(nextFactors);setAlerts(nextAlerts)}}catch(e){setError(e instanceof Error?e.message:'Risk assessment unavailable')}finally{setBusy(false)}}useEffect(()=>{load(false)},[]);
 return <><div className="page-header"><div><div className="eyebrow">CASEWORK / RISK INTELLIGENCE</div><h1>Investigative risk posture</h1><p className="muted">{caseData.title} · assessment is evidence-backed and not a legal determination.</p></div><button className="primary" type="button" onClick={()=>load(true)} disabled={busy}>{busy?'Calculating deterministic posture…':'Assess persisted evidence'}</button></div>{error&&<div className="error" role="alert">{error}</div>} {!assessment?<div className="surface"><Empty label="No assessment available" text="Run an explicit assessment against the persisted trace. No score is inferred from the case alone."/></div>:<><section className="risk-hero surface"><div><span className="eyebrow blue-text">RISK POSTURE · VERSION {assessment.version}</span><div className="risk-score">{assessment.score}<small>/ 100</small></div><div className={'risk-band '+assessment.band.toLowerCase()}>{assessment.band} · INVESTIGATIVE POSTURE</div><p>{assessment.explanation}</p></div><div className="risk-hero-meta"><span>INVESTIGATIVE PRIORITY<strong>{assessment.priority}</strong></span><span>WATCH STATUS<strong>{assessment.watch_status}</strong></span><span>CALCULATED<strong>{new Date(assessment.calculated_at).toLocaleString()}</strong></span></div></section><div className="risk-grid"><section className="surface"><PanelTitle eyebrow="CONTRIBUTING SIGNALS" title={factors.length?factors.length+' evidence-backed factors':'No qualifying factors'}/>{factors.length?<div className="risk-factor-list">{factors.map(factor=><article className="risk-factor" key={factor.factor_id}><div className="risk-factor-top"><span className={'badge '+(factor.contribution>=20?'high':factor.contribution>=10?'medium':'neutral')}>{factor.contribution>0?'+'+factor.contribution:'0'}</span><strong>{factor.name}</strong><span className="badge neutral">{factor.confidence_level}</span></div><p>{factor.explanation}</p><small>{factor.evidence_ids.length} evidence · {factor.transaction_hashes.length} transactions · {factor.pattern_ids.length} pattern observations</small></article>)}</div>:<Empty label="No evidence-backed factors" text="Factors are only created when persisted observations include evidence references."/>}</section><aside className="surface"><PanelTitle eyebrow="RISK DELTA" title="Why did the posture change?"/>{delta?<dl className="risk-delta"><dt>PREVIOUS</dt><dd>{delta.previous_score??'—'}</dd><dt>CURRENT</dt><dd>{delta.current_score}</dd><dt>DELTA</dt><dd className={delta.delta>=0?'delta-up':'delta-down'}>{delta.delta>=0?'+':''}{delta.delta}</dd><dt>NEW FACTORS</dt><dd>{delta.new_factors.length?delta.new_factors.join(', '):'None'}</dd><dt>REMOVED</dt><dd>{delta.removed_factors.length?delta.removed_factors.join(', '):'None'}</dd></dl>:<Empty label="First assessment" text="A later assessment will expose score and factor changes against this version."/>}<button className="secondary wide" type="button" onClick={()=>onNavigate('graph')}>Open evidence graph →</button></aside></div><section className="surface"><PanelTitle eyebrow="ALERT CANDIDATES" title="New investigative signals"/>{alerts.length?alerts.map(alert=><div className="risk-alert" key={alert.candidate_id}><span className="integration-status simulated"/><div><strong>{alert.trigger}</strong><p>{alert.risk_delta>=0?'+':''}{alert.risk_delta} posture delta · {alert.evidence_ids.length} evidence references</p></div><span className="badge neutral">{alert.status}</span></div>):<Empty label="No alert candidates" text="Candidates are created only when a later assessment records a positive evidence-backed change."/>}</section></>}</>
}
