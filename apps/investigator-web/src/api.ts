import type {Attribution,Case,Pattern,PatternSummary,Trace} from './types';
const API='http://localhost:8000';
async function request<T>(path:string,init?:RequestInit):Promise<T>{const response=await fetch(API+path,init);if(!response.ok){let message='Request failed';try{message=(await response.json()).detail||message}catch{}throw new Error(message)}return response.json()}
export const api={createCase:(body:{title:string;fraud_type:string;priority:string})=>request<Case>('/api/v1/cases',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)}),addWallet:(id:string,address:string)=>request<Case>('/api/v1/cases/'+id+'/wallets',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({address,chain:'ethereum'})}),trace:(id:string,address:string)=>request<Trace>('/api/v1/cases/'+id+'/traces',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({address,chain:'ethereum',direction:'forward',max_hops:3,max_nodes:100,max_edges:500,max_transactions:500})}),getCase:(id:string)=>request<Case>('/api/v1/cases/'+id),attribution:(chain:string,address:string)=>request<Attribution>('/api/v1/addresses/'+chain+'/'+address+'/attribution'),analyzePatterns:(caseId:string,traceId:string)=>request<Pattern[]>('/api/v1/cases/'+caseId+'/patterns/analyze',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({trace_id:traceId})}),patterns:(caseId:string,traceId:string)=>request<Pattern[]>('/api/v1/cases/'+caseId+'/patterns?trace_id='+encodeURIComponent(traceId)),patternSummary:(caseId:string,traceId:string)=>request<PatternSummary>('/api/v1/cases/'+caseId+'/patterns/summary?trace_id='+encodeURIComponent(traceId)),capabilities:()=>request<{name:string;status:string;mode?:string;note:string}[]>('/api/v1/provider/capabilities')};
